import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import App from './App';

describe('App', () => {
  it('should start at 0', () => {
    // Arrange / Act
    render(<App />);

    // Assert
  });
});
